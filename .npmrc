# Expose Astro dependencies for `pnpm` users
shamefully-hoist=true

# Allow peer dependency conflicts for Astro 6 compatibility
# (astro-embed and @astrojs/tailwind don't yet support v6 but work correctly)
legacy-peer-deps=true